#import ...

class CNN_skipCo(object):
    def __init__(self):
        #self.dataset = dataset

        self._build_model()

    def _build_model(self):
        #self.model = model
        pass

    def train(self):
        #self.history = history
        pass

    def predict(self):
        # we will see if we have to normalize the data(probabbly yes)
        #self.y_pred = self.model.predict(self.dataset.X_test)

        # denorm
        #self.y_pred_denorm = self.dataset.minmaxY.inverse_transform(self.y_pred)
        #self.y_test_denorm = self.dataset.minmaxY.inverse_transform(self.dataset.y_test)
        pass



