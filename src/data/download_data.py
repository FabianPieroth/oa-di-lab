#import ..


def download(file_path, url):
    # here we could automatically sync with the LRZ
    pass


def main(project_dir):
    # whatever we need to sync and do with the data before we can start with the code
    pass


if __name__ == '__main__':

    #project_dir = Path(__file__).resolve().parents[2]

    main(project_dir)
